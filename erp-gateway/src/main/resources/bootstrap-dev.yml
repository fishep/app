spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        port: 8859
        dashboard: localhost:8858